apiVersion: v1
kind: Service
metadata:
  namespace: argocd
  name: nginx-deployment
spec:
  selector:
    matchLabels:
      app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80